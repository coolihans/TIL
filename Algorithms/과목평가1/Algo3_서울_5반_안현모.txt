(1) a[middle] != key
(2) (start + end) / 2
(3) a[middle] == key
(4) middle
(5) middle

이진검색.
시작부터 끝까지 순서대로 검색하는 것보다 효율적인 검색법 중의 하나
리스트의 중간부터 검색한 후 키값이 중간값보다 어느 쪽에 위치하냐에 따라
middle 의 값을 다음 start 와 end 중 하나의 값에 할당하게 된다.
이를 반복해서 a[middle]의 값이 키값이 나올때까지 검색한다.
검색 실패의 경우 -1 반환.